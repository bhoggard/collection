- @work_rows.each do |row|
  .row.vertical-align
    - row.each do |work|
      .col-xs-12.col-sm-3
        .thumbnail
          = link_to(thumb_image_tag(work.images.first, alt_text(work)), work)
          .caption= work.artist.name
